config RTL88X2BU
	tristate "Realtek RTL88x2BU USB WiFi support"
	depends on USB && WLAN && CFG80211
	select WIRELESS_EXT
	help
	  Say Y here if you want to use the Realtek RTL88x2BU chipset
	  USB WiFi adapter.

